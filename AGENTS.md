# Project Agent Instructions

Read `PROJECT_CONTEXT.md` and `STUDY_PROGRESS.md` before doing anything else.

Continue from demonstrated progress instead of restarting the subject from zero.

Teach one manageable concept at a time using this sequence whenever possible:

1. prerequisite diagnostic
2. plain-language intuition
3. practical example
4. diagram or visualization
5. notation table
6. derivation
7. fully worked numerical
8. learner attempt
9. error diagnosis
10. exam-ready answer
11. closed-book mastery check

Define every mathematical symbol before using it.

Use the same practical example through multiple explanation layers when possible.

Update `STUDY_PROGRESS.md` only after checking performance with recall, a worked problem, or a short quiz.

If a new paper, sample paper, or assignment is attached, analyze it before changing priorities.

Treat instructor-provided slides, recordings, notebooks, source papers, and copyrighted material as local-only assets unless the user explicitly says otherwise.

Maintain Git safely:

- do not add instructor-provided files to tracked history
- stage only intended prep artifacts
- inspect `git status` before commit
- run the local skill validator after skill edits
- validate the visual lab after HTML/JS edits
